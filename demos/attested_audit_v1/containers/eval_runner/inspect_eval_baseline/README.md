This directory is the baked public eval baseline for the attested-audit demo.

For the operational prototype it records the intended Inspect/Inspect Evals
baseline identity and gives eval patches a concrete tree to apply against.
The current runner still emits the XSTest smoke result while the workflow is
being migrated to execute Inspect IFEval directly.
